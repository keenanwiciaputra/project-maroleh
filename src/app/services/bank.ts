import { LocationChangeListener } from '@angular/common';

export class Bank {
    id: string;
    nama: string;
    logo: string;
    nomor: string;
    constructor(id: string, nama: string, logo: string, nomor: string) {}
}
